services:
  app:
    build: .
    ports:
      - "11010:11010"
    volumes:
      - data:/app/data
    environment:
      - DATABASE_URL=/app/data/radar.db
    restart: unless-stopped

volumes:
  data:
